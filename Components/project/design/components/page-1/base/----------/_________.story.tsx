import {_________ as Component} from 'components/page-1/base/----------';
import type {StoryObj, Meta} from '@storybook/react';

type Story = StoryObj<typeof Component>;

const meta: Meta<typeof Component> = {
  title: 'Page 1/_________',
  component: Component,
};

export const _________: Story = {
  // ...
};

export default meta;
